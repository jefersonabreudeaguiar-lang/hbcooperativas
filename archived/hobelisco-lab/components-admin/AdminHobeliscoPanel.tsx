"use client";

import { Card } from "@/components/ui/Card";

export function AdminHobeliscoPanel() {
  return (
    <Card title="Hobelisco (lab)">
      <p className="text-sm text-gray-600">
        Painel de laboratório não incluído neste deploy. O app cooperativa e o livro caixa funcionam normalmente.
      </p>
    </Card>
  );
}
